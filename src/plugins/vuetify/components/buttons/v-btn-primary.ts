export default {
  // props
  variant: 'default',
  baseColor: 'primary',
  rounded: true,

  // classes
  class: [
    'v-btn--primary',
  ],

  // styles
  style: [
    'text-transform: uppercase;',
  ],
}